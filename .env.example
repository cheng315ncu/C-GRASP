# =============================================================================
# CGRASP - Environment Configuration Example
# =============================================================================
# Copy this file to .env and modify as needed:
#   cp .env.example .env
#
# All paths can be absolute or relative to the project root.
# =============================================================================

# -----------------------------------------------------------------------------
# Data Paths
# -----------------------------------------------------------------------------

# Input CSV file containing HRV features
# Required columns: subject, trial, MeanRR_ms, SDNN_ms, RMSSD_ms, etc.
CGRASP_CSV_PATH=data/HRV_all_final.csv

# Output directory for generated reports
# Reports will be saved as {OUTPUT_DIR}/{subject}/{trial}.txt
CGRASP_OUTPUT_DIR=outputs/reports

# -----------------------------------------------------------------------------
# RAG Configuration
# -----------------------------------------------------------------------------

# Directory containing clinical PDF papers for knowledge retrieval
CGRASP_PDF_DIR=clinical_pdfs

# ChromaDB vector store directory (will be created automatically)
CGRASP_CHROMA_DIR=chroma_db

# -----------------------------------------------------------------------------
# Model Configuration
# -----------------------------------------------------------------------------

# HuggingFace model ID for the main LLM
# Options:
#   - Qwen/Qwen3-VL-4B-Instruct      (lighter, ~8GB VRAM)
#   - Qwen/Qwen3-VL-8B-Instruct      (recommended, ~16GB VRAM)
#   - google/medgemma-4b-it          (medical-specialized)
#   - Cannae-AI/MedicalQwen3-Reasoning-14B-IT (larger, ~28GB VRAM)
CGRASP_MODEL_ID=Qwen/Qwen3-VL-8B-Instruct

# -----------------------------------------------------------------------------
# Optional: HuggingFace Configuration
# -----------------------------------------------------------------------------

# HuggingFace token (required for some gated models)
# HF_TOKEN=your_huggingface_token_here

# HuggingFace cache directory
# HF_HOME=/path/to/huggingface/cache

# -----------------------------------------------------------------------------
# Optional: CUDA Configuration
# -----------------------------------------------------------------------------

# Specify which GPU to use (0-indexed)
# CUDA_VISIBLE_DEVICES=0

# Enable TF32 for faster computation on Ampere+ GPUs
# TORCH_ALLOW_TF32_CUBLAS_OVERRIDE=1
